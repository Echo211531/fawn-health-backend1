# 小鹿健康后端系统 - 项目文档

## 📋 目录
- [项目概述](#项目概述)
- [技术架构](#技术架构)
- [模块划分](#模块划分)
- [数据库设计](#数据库设计)
- [核心功能模块](#核心功能模块)
- [技术栈详解](#技术栈详解)
- [项目结构](#项目结构)
- [部署说明](#部署说明)

---

## 项目概述

**项目名称**：fawn-health-backend（小鹿健康后端系统）  
**项目描述**：面向高校学生与年轻群体的全平台健康管理App后端服务  
**技术栈**：Java 21 + Spring Boot 3.4.4  
**开发模式**：前后端分离架构

### 核心定位
- 🧍 **个体健康档案管理**：记录身高、体重、BMI、体脂率等关键指标
- 🍱 **饮食记录与营养分析**：AI图像识别和自然语言处理实现营养估算
- 🧠 **智能推荐与健康建议**：基于大模型的个性化饮食建议与作息改善计划
- 💬 **社区互动与社群激励**：发布动态、评论互动、点赞打卡

---

## 技术架构

### 整体架构图
```
┌─────────────────────────────────────────────────────────────┐
│                     前端应用层 (App/Web)                      │
└──────────────────────────┬──────────────────────────────────┘
                           │ HTTP/REST API
┌──────────────────────────▼──────────────────────────────────┐
│                    Spring Boot 应用层                         │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │  Controller  │  │   Service    │  │   Mapper     │      │
│  │    层        │  │     层       │  │     层       │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐   │
│  │          AI智能模块 (Spring AI + RAG)                │   │
│  │  - HealthApp (健康问答)                              │   │
│  │  - HealthReportApp (健康报告生成)                    │   │
│  │  - ReActAgent (智能体)                               │   │
│  │  - RAG向量检索 (PGVector)                            │   │
│  └──────────────────────────────────────────────────────┘   │
└──────────────────────────┬──────────────────────────────────┘
                           │
        ┌──────────────────┼──────────────────┐
        │                  │                  │
┌───────▼──────┐  ┌────────▼────────┐  ┌──────▼──────┐
│   MySQL      │  │   PostgreSQL   │  │   MongoDB   │
│  (业务数据)   │  │  (PGVector)    │  │  (聊天记录)  │
└──────────────┘  └────────────────┘  └─────────────┘
        │                  │                  │
┌───────▼──────┐  ┌────────▼────────┐  ┌──────▼──────┐
│   Redis      │  │   RabbitMQ     │  │  阿里云OSS   │
│  (缓存/会话)  │  │  (消息队列)     │  │  (文件存储)  │
└──────────────┘  └─────────────────┘  └─────────────┘
```

### 技术选型

#### 核心框架
- **Spring Boot 3.4.4**：主框架
- **Java 21**：JDK版本
- **MyBatis Plus 3.5.12**：ORM框架
- **Spring Session + Redis**：会话管理

#### AI与智能服务
- **Spring AI 1.0.0-M6**：AI框架
- **阿里云百炼 (DashScope)**：大模型服务（qwen-plus）
- **PGVector**：向量数据库（RAG知识库）
- **MCP (Model Context Protocol)**：模型上下文协议
- **EasyRules 4.1.0**：规则引擎

#### 数据存储
- **MySQL 8.0+**：主数据库（业务数据）
- **PostgreSQL + PGVector**：向量数据库（知识库）
- **MongoDB**：文档数据库（聊天记录、消息存储）
- **Redis**：缓存、分布式锁、会话存储
- **Caffeine**：本地缓存

#### 中间件与服务
- **RabbitMQ**：消息队列（异步任务、订单处理）
- **Redisson 3.21.3**：分布式锁
- **阿里云OSS**：对象存储（图片、文件）
- **JWT**：身份认证

#### 工具库
- **Hutool 5.8.37**：Java工具类库
- **Lombok**：简化代码
- **Knife4j 4.4.0**：API文档（Swagger）
- **iText 9.1.0**：PDF生成
- **ZXing**：二维码生成
- **FastJSON 1.2.73**：JSON处理

#### 第三方服务
- **百度AI SDK**：图像识别
- **支付宝SDK**：支付集成
- **Spring Mail**：邮件服务

---

## 模块划分

### 1. 用户管理模块 (`user`)
**路径**：`com.ljh.fawnhealth.controller.UserController`  
**功能**：
- 用户注册/登录（邮箱验证）
- 用户信息管理（昵称、头像、个人资料）
- 健康档案管理（身高、体重、BMI、目标体重）
- 体重历史记录
- VIP会员管理
- 登录日志记录

**核心实体**：
- `User`：用户基本信息
- `UserWeightHistory`：体重历史
- `UserLoginLog`：登录日志
- `VipOrders`：VIP订单
- `VipBenefits`：VIP权益

### 2. 饮食管理模块 (`food`)
**路径**：
- `com.ljh.fawnhealth.controller.FoodLibraryController`：食物库管理
- `com.ljh.fawnhealth.controller.FoodDiaryController`：饮食记录

**功能**：
- 食物库管理（食物信息、营养成分）
- 食物分类管理
- 饮食记录（早餐、午餐、晚餐、加餐）
- 营养分析（热量、蛋白质、脂肪、碳水化合物）
- 饮食图片上传与识别

**核心实体**：
- `FoodLibrary`：食物库
- `FoodCategories`：食物分类
- `DietRecords`：饮食记录主表
- `DietFoodItems`：饮食记录食物项

### 3. AI智能模块 (`ai`)
**路径**：`com.ljh.fawnhealth.ai.*`  
**功能**：
- **健康问答** (`HealthApp`)：基于RAG的智能问答
- **健康报告生成** (`HealthReportApp`)：生成个性化健康报告
- **智能体** (`ReActAgent`, `ToolCallAgent`)：工具调用智能体
- **RAG检索**：基于PGVector的向量检索
- **工具集合** (`tool`)：数据库操作、文件操作、PDF生成等

**核心组件**：
```
ai/
├── agent/          # 智能体实现
│   ├── ReActAgent.java      # ReAct智能体
│   ├── ToolCallAgent.java   # 工具调用智能体
│   └── FawnManus.java       # 主智能体
├── app/            # AI应用
│   ├── HealthApp.java       # 健康问答应用
│   └── HealthReportApp.java # 健康报告应用
├── rag/            # RAG检索增强
│   ├── HealthAppDocumentLoader.java
│   └── QueryRewriter.java
├── tool/           # 工具集合
│   ├── DatabaseOperationTool.java
│   ├── DietTool.java
│   ├── PDFGenerationTool.java
│   └── ...
└── store/           # 存储
    ├── MongoChatMemory.java
    └── ChatStreamEventStore.java
```

### 4. 社区互动模块 (`community`)
**路径**：
- `com.ljh.fawnhealth.controller.CommunityPostsController`：帖子管理
- `com.ljh.fawnhealth.controller.CommentController`：评论管理

**功能**：
- 社区帖子发布、编辑、删除
- 帖子点赞、收藏
- 多级评论系统（支持回复）
- 评论点赞
- 内容审核（敏感词过滤）

**核心实体**：
- `CommunityPosts`：社区帖子
- `PostLikes`：帖子点赞
- `Comments`：评论（支持多级嵌套）
- `CommentLikes`：评论点赞

### 5. 健康评估模块 (`health`)
**路径**：
- `com.ljh.fawnhealth.controller.HealthAssessmentController`：健康评估
- `com.ljh.fawnhealth.controller.HealthRiskWarningController`：风险预警
- `com.ljh.fawnhealth.controller.InterventionPlanController`：干预计划

**功能**：
- 健康评估（基于用户数据）
- 健康风险预警（定时扫描）
- 干预计划生成
- 健康报告生成（PDF导出）
- 健康规则引擎（EasyRules）

**核心实体**：
- `HealthAssessment`：健康评估
- `HealthRiskWarning`：健康风险预警
- `InterventionPlan`：干预计划
- `HealthReport`：健康报告
- `HealthRule`：健康规则
- `AdviceRules`：建议规则

### 6. 商品与订单模块 (`order`)
**路径**：
- `com.ljh.fawnhealth.controller.ProductController`：商品管理
- `com.ljh.fawnhealth.controller.OrderController`：订单管理
- `com.ljh.fawnhealth.controller.CartController`：购物车

**功能**：
- 商品管理（商品信息、分类、库存）
- 购物车管理
- 订单创建、支付、退款
- 订单状态流转
- 订单操作日志

**核心实体**：
- `Product`：商品
- `ProductCategory`：商品分类
- `Cart`：购物车
- `Order`：订单
- `OrderItem`：订单项
- `OrderOperationLog`：订单操作日志
- `RefundApplication`：退款申请

### 7. 优惠券模块 (`coupon`)
**路径**：`com.ljh.fawnhealth.controller.CouponsController`  
**功能**：
- 优惠券配置（满减、折扣、无门槛）
- 优惠券发放（手动领取、兑换码）
- 优惠券使用（订单抵扣）
- 优惠券使用日志
- 优惠券作用范围管理

**核心实体**：
- `Coupons`：优惠券配置
- `CouponsScope`：优惠券作用范围
- `UserCoupon`：用户优惠券
- `CouponUsageLogs`：使用日志
- `ExchangeCode`：兑换码

### 8. 支付模块 (`payment`)
**路径**：`com.ljh.fawnhealth.controller.AliPayController`  
**功能**：
- 支付宝支付集成
- 支付回调处理
- 支付状态查询
- 退款处理

### 9. 文件管理模块 (`file`)
**路径**：`com.ljh.fawnhealth.controller.FileController`  
**功能**：
- 文件上传（阿里云OSS）
- 图片上传与处理
- 文件下载

### 10. 地址管理模块 (`address`)
**路径**：`com.ljh.fawnhealth.controller.AddressController`  
**功能**：
- 收货地址管理
- 默认地址设置

**核心实体**：
- `Address`：收货地址

---

## 数据库设计

### 核心数据表

#### 用户相关
- **user**：用户基本信息表
  - 字段：id, email, username, password, avatar, nickname, role, gender, birthday, height, weight, target_weight, bmi, daily_calories, is_vip, vip_expire_time, vip_level, status
  - 索引：email(唯一), nickname, vip状态, role

- **user_weight_history**：体重历史记录表
  - 字段：id, user_id, weight, bmi, record_date, create_time

- **user_login_log**：用户登录日志表
  - 字段：id, user_id, login_time, login_ip, device_info

#### 饮食相关
- **food_library**：食物库表
  - 字段：id, name, category_id, category_name, calories, protein, fat, carbohydrate, fiber, image, is_common
  - 索引：name, category_id

- **food_categories**：食物分类表
  - 字段：id, name, icon, description, sort_order, status

- **diet_records**：饮食记录主表
  - 字段：id, user_id, meal_type, record_date, record_time, total_calories, note
  - 索引：user_id + record_date

- **diet_food_items**：饮食记录食物项表
  - 字段：id, record_id, food_id, food_name, amount, unit, calories, protein, fat, carbohydrate, images, note
  - 外键：record_id -> diet_records.id

#### 社区相关
- **community_posts**：社区帖子表
  - 字段：id, user_id, title, content, images, like_count, comment_count, view_count, status, is_delete
  - 索引：user_id, status, create_time

- **post_likes**：帖子点赞表
  - 字段：id, post_id, user_id
  - 唯一索引：post_id + user_id

- **comments**：评论表（支持多级嵌套）
  - 字段：id, post_id, user_id, parent_id, root_id, reply_to_user_id, content, like_count, is_author, status
  - 索引：post_id, user_id, parent_id, root_id

- **comment_likes**：评论点赞表
  - 字段：id, comment_id, user_id
  - 唯一索引：comment_id + user_id

#### 健康相关
- **health_assessment**：健康评估表
  - 字段：id, user_id, assessment_date, bmi, health_score, risk_level, suggestions

- **health_risk_warning**：健康风险预警表
  - 字段：id, user_id, risk_type, risk_level, warning_message, is_handled, handle_time

- **intervention_plan**：干预计划表
  - 字段：id, user_id, plan_type, plan_content, start_date, end_date, status

- **health_report**：健康报告表
  - 字段：id, user_id, report_date, report_content, pdf_url

- **health_rule**：健康规则表
  - 字段：id, rule_name, rule_condition, rule_action, priority, status

- **advice_rules**：建议规则表
  - 字段：id, rule_name, condition_expression, advice_content, priority

#### 商品订单相关
- **product**：商品表
  - 字段：id, name, category_id, description, price, original_price, stock, sales, main_image, sub_images, detail, specs, status, weight, is_hot, is_recommend, sort_order
  - 索引：category_id, status, sort_order

- **product_category**：商品分类表（支持多级）
  - 字段：id, parent_id, name, level, icon, description, sort_order, status
  - 索引：parent_id, sort_order

- **cart**：购物车表
  - 字段：id, user_id, product_id, quantity, selected, specs
  - 唯一索引：user_id + product_id

- **order**：订单表
  - 字段：id, order_no, user_id, total_amount, discount_amount, final_amount, payment_method, trade_no, payment_time, status, address_id, coupon_id, remark
  - 索引：order_no(唯一), user_id, status, create_time

- **order_item**：订单项表
  - 字段：id, order_id, product_id, product_name, quantity, price, total_price, specs
  - 索引：order_id

- **order_operation_log**：订单操作日志表
  - 字段：id, order_id, operation_type, operation_desc, operator_id, create_time

- **refund_application**：退款申请表
  - 字段：id, order_id, user_id, refund_amount, refund_reason, status, process_time

#### VIP相关
- **vip_orders**：VIP订单表
  - 字段：id, order_no, user_id, vip_type, amount, discount_amount, final_amount, payment_method, trade_no, payment_time, status, start_time, end_time, coupon_id, is_renewal
  - 索引：order_no(唯一), user_id, status

- **vip_benefits**：VIP权益表
  - 字段：id, vip_type, benefit_code, benefit_name, price, description, value, sort_order, status

#### 优惠券相关
- **coupons**：优惠券配置表
  - 字段：id, name, type, discount_type, specific, discount_value, threshold_amount, max_discount_amount, obtain_way, issue_begin_time, issue_end_time, term_days, term_begin_time, term_end_time, status, total_num, issue_num, used_num, user_limit, code, description
  - 索引：code(唯一), 时间范围, status

- **coupons_scope**：优惠券作用范围表
  - 字段：id, coupon_id, scope_type, scope_value

- **user_coupon**：用户优惠券表
  - 字段：id, user_id, coupon_id, status, obtain_time, use_time, expire_time
  - 索引：user_id, coupon_id, status

- **coupon_usage_logs**：优惠券使用日志表
  - 字段：id, user_id, coupon_id, order_id, discount_amount, use_time

- **exchange_code**：兑换码表
  - 字段：id, coupon_id, code, status, use_time, user_id

#### 地址相关
- **address**：收货地址表
  - 字段：id, user_id, receiver_name, receiver_phone, province, city, district, detail_address, is_default, postal_code

---

## 核心功能模块

### 1. AI智能问答系统

#### 架构设计
```
用户输入
   ↓
HealthApp.doChat()
   ↓
RAG检索增强 (PGVector)
   ↓
ReActAgent (工具调用)
   ↓
大模型推理 (阿里云百炼 qwen-plus)
   ↓
流式返回结果
```

#### 核心组件
- **HealthApp**：健康问答主应用
  - 支持流式输出（SSE）
  - RAG检索增强
  - 工具调用能力
  - 聊天历史管理（MongoDB）

- **RAG检索**：
  - 文档加载：Markdown文档（food、hospital、knowledge、nutrition）
  - 向量化：PGVector存储
  - 检索策略：关键词增强 + 上下文查询增强

- **工具集合**：
  - `DatabaseOperationTool`：数据库操作
  - `DietTool`：饮食记录查询
  - `QueryUserTool`：用户信息查询
  - `PDFGenerationTool`：PDF生成
  - `FileOperationTool`：文件操作
  - `WebScrapingTool`：网页抓取
  - `ZhipuWebSearchTool`：智谱搜索

#### 知识库文档
- `document/food/`：食物相关（低糖食物、高蛋白食物、糖尿病饮食建议等）
- `document/hospital/`：医院信息（北京协和、上海瑞金、广州中山一院等）
- `document/knowledge/`：健康知识（BMI计算、血压管理、睡眠质量等）
- `document/nutrition/`：营养知识（营养素需求、维生素来源、热量摄入等）

### 2. 健康报告生成系统

#### 功能流程
```
用户数据收集
   ↓
健康评估计算
   ↓
风险预警扫描
   ↓
干预计划生成
   ↓
PDF报告生成 (iText)
   ↓
存储到OSS
```

#### 核心组件
- **HealthReportApp**：报告生成应用
- **HealthAssessmentService**：健康评估服务
- **HealthRiskWarningService**：风险预警服务
- **InterventionPlanService**：干预计划服务

### 3. 规则引擎系统

#### 技术实现
- **EasyRules 4.1.0**：规则引擎框架
- **MVEL表达式**：规则条件表达式
- **规则优先级**：支持规则优先级排序

#### 应用场景
- 健康风险评估规则
- 饮食建议规则
- 优惠券使用规则
- 订单处理规则

### 4. 消息队列系统

#### RabbitMQ配置
- **队列**：订单处理、邮件发送、数据同步
- **交换机**：Topic Exchange
- **消息确认**：手动确认模式

#### 核心Handler
- `PromotionMqHandler`：促销活动消息处理

### 5. 缓存策略

#### 多级缓存
1. **Caffeine本地缓存**：热点数据（用户信息、配置信息）
2. **Redis分布式缓存**：共享数据（排行榜、会话）
3. **数据库**：持久化存储

#### 缓存应用
- 用户信息缓存
- 食物库缓存
- 商品信息缓存
- 排行榜缓存

### 6. 文件存储系统

#### 阿里云OSS
- **存储内容**：用户头像、饮食图片、健康报告PDF
- **访问控制**：私有/公共读
- **CDN加速**：支持CDN加速访问

---

## 技术栈详解

### Spring AI集成

#### 配置
```yaml
spring:
  ai:
    dashscope:
      api-key: ${spring.ai.dashscope.api-key}
      chat:
        options:
          model: qwen-plus
    mcp:
      client:
        stdio:
          servers-configuration: classpath:mcp-servers.json
```

#### MCP服务器
- **amap-maps**：高德地图MCP服务
- **fawn-image-search-mcp-server**：图像搜索MCP服务（自定义）

### 向量数据库 (PGVector)

#### 配置
- **数据源**：PostgreSQL + PGVector扩展
- **向量维度**：1536（适配OpenAI embedding）
- **索引类型**：HNSW索引（高性能近似最近邻搜索）

#### 应用
- RAG知识库向量存储
- 语义相似度检索
- 上下文增强查询

### MongoDB应用

#### 存储内容
- 聊天记录（`MongoChatMemory`）
- 流式事件存储（`ChatStreamEventStore`）
- 消息序列化（`MessageSerializer`）

### 安全机制

#### JWT认证
- **Token生成**：用户登录后生成JWT
- **Token验证**：`JwtTokenAdminInterceptor`拦截器
- **Token存储**：Redis Session

#### 敏感词过滤
- **文件**：`banned_words.txt`, `pub_banned_words.txt`
- **AOP拦截**：`ProhibitedWordAdvisor`
- **实时检测**：用户输入内容检测

### 定时任务

#### 调度器
- **@EnableScheduling**：启用定时任务
- **HealthRiskScheduledScanner**：健康风险定时扫描

---

## 项目结构

```
fawn-health-backend/
├── src/main/java/com/ljh/fawnhealth/
│   ├── ai/                    # AI智能模块
│   │   ├── agent/            # 智能体
│   │   ├── app/              # AI应用
│   │   ├── config/           # AI配置
│   │   ├── context/          # 上下文管理
│   │   ├── model/            # AI模型
│   │   ├── prompt/           # 提示词
│   │   ├── rag/              # RAG检索
│   │   ├── store/            # 存储
│   │   └── tool/             # 工具集合
│   ├── commen/               # 公共类
│   ├── config/               # 配置类
│   ├── constant/             # 常量定义
│   ├── context/              # 上下文
│   ├── controller/           # 控制器层
│   ├── events/               # 事件定义
│   ├── exception/            # 异常处理
│   ├── handler/              # 消息处理器
│   ├── interceptor/          # 拦截器
│   ├── manager/              # 管理器
│   ├── mapper/               # MyBatis Mapper接口
│   ├── model/                # 数据模型
│   │   ├── dto/              # 数据传输对象
│   │   ├── entity/           # 实体类
│   │   ├── enums/            # 枚举类
│   │   ├── query/            # 查询对象
│   │   └── vo/               # 视图对象
│   ├── mq/                   # 消息队列
│   ├── properties/           # 配置属性
│   ├── scheduler/            # 定时任务
│   ├── service/              # 服务层
│   │   └── impl/             # 服务实现
│   ├── strategy/             # 策略模式
│   └── utils/                # 工具类
├── src/main/resources/
│   ├── mapper/               # MyBatis XML映射文件
│   ├── document/             # 知识库文档
│   │   ├── food/             # 食物相关
│   │   ├── hospital/         # 医院信息
│   │   ├── knowledge/        # 健康知识
│   │   └── nutrition/        # 营养知识
│   ├── templates/            # 模板文件
│   ├── application.yml       # 主配置文件
│   ├── application-local.yml # 本地配置
│   ├── mcp-servers.json      # MCP服务器配置
│   ├── banned_words.txt      # 敏感词库
│   └── logback.xml           # 日志配置
├── sql/
│   └── create.sql            # 数据库初始化脚本
├── fawn-image-search-mcp-server/  # MCP图像搜索服务
├── pom.xml                   # Maven配置
└── README.md                 # 项目说明
```

---

## 部署说明

### 环境要求
- **JDK**：Java 21+
- **Maven**：3.6+
- **MySQL**：8.0+
- **PostgreSQL**：14+（需安装PGVector扩展）
- **MongoDB**：5.0+
- **Redis**：6.0+
- **RabbitMQ**：3.8+

### 配置步骤

1. **数据库初始化**
   ```sql
   -- 执行 sql/create.sql 初始化MySQL数据库
   -- 创建PostgreSQL数据库并安装PGVector扩展
   CREATE EXTENSION vector;
   ```

2. **配置文件**
   - 复制 `application-local.yml` 并配置：
     - 数据库连接
     - Redis连接
     - MongoDB连接
     - 阿里云OSS配置
     - 支付宝配置
     - 邮件服务配置
     - AI API密钥

3. **启动服务**
   ```bash
   mvn clean install
   mvn spring-boot:run
   ```

4. **API文档**
   - 访问：`http://localhost:8112/api/swagger-ui.html`
   - Knife4j文档界面

### 关键配置项

#### 数据库配置
```yaml
spring:
  datasource:
    mysql:
      url: jdbc:mysql://localhost:3306/fawn_health
      username: root
      password: your_password
    postgres:
      url: jdbc:postgresql://localhost:5432/fawn_health_vector
      username: postgres
      password: your_password
```

#### Redis配置
```yaml
spring:
  data:
    redis:
      host: localhost
      port: 6379
      password: your_password
      database: 0
```

#### AI配置
```yaml
spring:
  ai:
    dashscope:
      api-key: your_dashscope_api_key
```

---

## 总结

### 项目特点
1. **智能化**：集成Spring AI，支持RAG检索、工具调用、智能体
2. **模块化**：清晰的分层架构，易于维护和扩展
3. **高性能**：多级缓存、消息队列、数据库优化
4. **可扩展**：支持MCP协议、规则引擎、策略模式

### 技术亮点
- ✅ Spring Boot 3 + Java 21 最新技术栈
- ✅ Spring AI集成，支持大模型应用
- ✅ RAG检索增强，知识库向量化
- ✅ 多数据源支持（MySQL、PostgreSQL、MongoDB）
- ✅ 完整的电商功能（商品、订单、支付、优惠券）
- ✅ 健康管理闭环（记录、分析、评估、预警、干预）

